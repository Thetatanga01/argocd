# nindent 20
traefik:
  redis:
    ingress:
      routes:
        - match: HostSNI(`*`)
          services:
            - name: redis-haproxy
              port: 6379
              terminationDelay: 60000        
              nativeLB: true

  haproxy:
    ingress:
      routes:
        - kind: Rule
          match: Host(`redis-haproxy.rsm-izm.vodafone.local`)
          services:
            - name: redis-haproxy
              port: 8080     