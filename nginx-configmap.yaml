apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-index
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>NGINX</title>
    </head>
    <body><center>
      <h1>{{ .ClusterName }}</h1>
      <p>Worker Node: {{ .NodeName }}</p>
      <p>Pod IP: {{ .PodIP }}</p>
      </center>
    </body>
    </html>
